import './index.css';

const NavDrawer = () => {
  return (
    <div className="navDrawer">
      <nav>
        <ul className='navList'>
          <li>
            <button>Home</button>
          </li>
        </ul>
      </nav>
    </div>
  );
};

export default NavDrawer;
